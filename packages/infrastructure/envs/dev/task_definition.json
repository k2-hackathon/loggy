[
    {
        "name": "${container_nginx_name}",
        "image": "${container_nginx_image}",
        "cpu": 0,
        "essential": true,
        "portMappings": [
            {
                "containerPort": 80,
                "hostPort": 80
            }
        ]
    },
    {
        "name": "${container_fastapi_name}",
        "image": "${container_fastapi_image}",
        "cpu": 0,
        "essential": true,
        "portMappings": []
    }
]